# Database Structure for minomp

DB_TYPE=mysql
DB_NAME=minomp

# Table: users
# Columns: id, username, email, password, role, referral_code, country_code_id, mobileno, is_deleted, token, language_id, refresh_token, created_at, updated_at

# Table: proposals
# Columns: id, user_id, proposal_name, reward_id, minomp_time, break_time, status, is_deleted, is_received_reward, start_datetime, end_datetime, reward_type, created_date, updated_date

# Table: logs
# Columns: id, request_path, request_method, request_data, log_message, log_messagetype, response_data, request_datetime, response_datetime, user_id

# Table: rewards
# Columns: id, reward_name, reward_icon, is_active

# Table: custome_rewards
# Columns: id, reward_name, reward_icon, userid

# Table: points
# Columns: id, user_id, points_earned, referral_code_id

# Table: country_codes
# Columns: id, country_name, country_code, is_active

# Table: sessions
# Columns: id, user_id, session_id, device_id, latitude, longitude, login_datetime, logout_datetime

# Table: connections
# Columns: id, parent_id, kid_id, status

# Table: referrals
# Columns: id, referred_by_id, referred_to_id, create_date, updated_date, referral_code, is_active

# Table: time_tracking
# Columns: id, proposal_id, start_time, end_time, break_time_used, create_date, updated_date

# Table: otp
# Columns: id, code, userid, expiredatetime, create_datetime, is_used, count, is_expired

# Table: proposal_mapping
# Columns: id, kid_id, parent_id, proposal_id, status, request_datetime, updated_datetime

# Table: notificationtype
# Columns: ID, Name, CreatedBy, CreatedDate, UpdatedBy, UpdatedDate, Status

# Table: notification
# Columns: id, notification_typeid, senderid, receiverid, notification_data, created_date, expired_date, updated_date, is_read, is_deleted

# Table: languages
# Columns: id, language, is_active

# Table: devices
# Columns: id, device_model, user_id, device_type, AppVersion, OsVersion

# Table: todos
# Columns: id, title, description, completed, created_at, updated_at

# Table: user_photos
# Columns: id, user_id, photo, created_at
